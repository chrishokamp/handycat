// this service works with the logger directive to keep track of all user actions
angular.module('services')
.factory('actionLogger', ['$rootScope', '$log', function($rootScope, $log) {

  var loggerEndpoint = 'http://'





}]);
